# FC온 리그사이트

> 📁 모든 프로젝트 문서는 `docs/` 폴더에 있습니다.

```
docs/README.md          ← 여기서 시작하세요
```

## 빠른 시작

```bash
# 백엔드
cd backend && npm install && cp .env.example .env && npm run dev

# 프론트엔드 (새 터미널)
cd frontend && npm install && npm run dev
```
